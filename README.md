# Customer Segmentation
Local demo with MLOps for customer segmentation:
- Postgres as storage
- MLflow for experiment tracking + model registry
- Airflow for orchestration (train + batch scoring)
- Streamlit for interactive demo (manual input / random user + offer + PCA plot)
